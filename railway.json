{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && cd artifacts/new-world && pnpm build && cd ../api-server && pnpm build"
  },
  "deploy": {
    "startCommand": "cd artifacts/api-server && SERVE_STATIC_PATH=../new-world/dist/public PORT=$PORT node --enable-source-maps ./dist/index.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
